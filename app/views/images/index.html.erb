<div class="container my-5">
    <!-- <div class="card-columns">
    <% @images.each do |image| %>
        <div class="card">
        <div class="card-body p-1">
            <%= image_tag image.image, class: 'd-block w-100' %>

            <p class="my-1"><%= image.description %></p>

            <%= link_to "View Image", image_path(image.id), class: 'btn btn-secondary stretched-link' %>
        </div>
        </div>
    <% end %>
    </div> -->

    <div class="d-flex justify-content-center col-12">
        <div class="row">
            <% @images.each do |image| %>
                <div class="col-3">
                    <%= link_to image_tag(image.image, class: "image-user-index mt-3"), image_path(image.id) %>
                    <% if current_user %>
                        <div class="container d-flex">
                            <div class="col-11">
                                <% like = current_user.likes.find_by(image: image) %>
                                <% if like.nil? %>
                                    <form action="/likes" method="post">
                                        <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
                                        <input type="hidden" name="like[image_id]" value="<%= image.id %>">
                                        
                                        <button class="btn" type="submit" value="Like">
                                            <i class="far fa-heart"></i>
                                        </button>
                                    </form>
                                <% else %>
                                    <form action="<% like_path(like) %>" method="post">
                                        <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
                                        <input type="hidden" name="_method" value="DELETE">
                                        
                                        <button class="btn" type="submit" value="Unlike">
                                            <i class="far fa-heart text-danger"></i>
                                        </button>
                                    </form>
                                <% end %>
                            </div>
                            <div>
                                <i class="far fa-bookmark"></i>
                            </div>
                        </div>
                    <% end %>
                    <div class="container d-flex mt-2">
                        <% if image.user.avatar.attached? %>
                            <%= link_to image_tag(image.user.avatar, class: "mini"), profile_path(image.user.username) %>
                        <% else %>
                            <%= link_to image_tag("/images/no_photo.png", :class => "mini"), profile_path(image.user.username) %>
                        <% end %>
                        <p class="fw-bold ms-2 mt-1"><%= link_to image.user.username, profile_path(image.user.username), class: "search-text" %></p>
                        <p class="mx-2 mt-1"><%= image.description %></p><br>
                    </div>
                    <div class="text-end container">
                        <small class="text-muted ms-4"><%= time_ago_in_words image.created_at %> ago</small>
                        <%#= link_to "View Image", image_path(image.id), class: "btn btn-secondary" %>
                    </div>
                </div>
            <% end %>
        </div>
    </div>
</div>